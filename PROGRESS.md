# Progress

Tracks the 27-commit plan for the AQUA self-pruning network. One row per
commit. Mark `[x]` only once the commit is actually made (code + tests
passing + committed), not when merely started.


| # | Commit | Status | Notes |
|---|--------|--------|-------|
| 1 | chore: repo scaffold | [x] | `5be28ad`; gap (missing seed docstring) closed in `53ce2d4` |
| 2 | feat(engine): Tensor wrapping ndarray, parent tracking, grad storage | [x] | `b83d4ff` |
| 3 | feat(engine): elementwise add/sub/mul/div with unbroadcast backward | [x] | `cb3f42c`; comments trimmed in `786f8f9`. Verified: forward, bias-broadcast unbroadcast, shared-node (`x+x`) accumulation, finite-diff cross-check |
| 4 | feat(engine): matmul + sum/mean reductions | [x] | `c9e2ae5`. Verified: matmul backward vs finite-diff, sum over axis=None/0/1 + keepdims, mean divide-by-N vs finite-diff, chained matmul->mean backward |
| 5 | feat(engine): backward() with topo-sort + grad accumulation | [x] | `bf362a1`. Verified: full multi-op pipeline vs finite-diff, diamond/shared-node accumulation, requires_grad=False branch (no crash, no spurious grad), repeated backward() accumulates |
| 6 | test(engine): finite-difference gradient checks for all ops | [x] | `tests/test_engine_ops.py`, 23 tests passing: add/sub/mul/div (plain + 2 broadcast shapes), matmul, sum/mean (axis+keepdims variants), shared-node accumulation, requires_grad=False skip, repeated-backward accumulation |
| 7 | feat(engine): ReLU + tanh | [x] | `engine/activations.py`; gradcheck added immediately in `tests/test_activations.py` (3 tests, 26 total passing) rather than deferred |
| 8 | feat(engine): fused numerically-stable softmax cross-entropy | [x] | `engine/loss.py`; manually verified vs naive reference, finite-diff, hand-derived (probs-one_hot)/N, and overflow stress test (naive softmax NaNs, ours stays finite). Formal pytest deferred to commit 9 as planned |
| 9 | test(engine): grad-check softmax-CE + broadcasting bias case | [x] | `tests/test_softmax_ce.py`: gradcheck across 3 shape/label configs, large-logit finite-value stress test, and a full matmul+bias-broadcast+softmax-CE pipeline through one real backward() call (31 tests total passing) |
| 10 | feat(nn): Linear layer + He init | [x] | `nn/linear.py`; `tests/test_linear.py`: forward shape, end-to-end gradcheck through `__call__` (x, W, b all vs finite-diff), He-init std statistical check, `parameters()` (35 tests total passing) |
| 11 | feat(nn): Sequential MLP container | [x] | `nn/sequential.py` + `nn/activations.py` (ReLU wrapped as a parameter-free module so Sequential can chain it like Linear). `tests/test_sequential.py`: shape flow, parameters() collects only Linear's, and a full Linear-ReLU-Linear-softmaxCE end-to-end gradcheck (38 tests total passing) |
| 12 | feat(optim): SGD-with-momentum | [x] | `optim/sgd.py`; `tests/test_sgd.py`: zero-momentum vs plain GD, multi-step velocity recursion vs hand computation, no-grad skip, zero_grad, and a real training loop that reduces loss over 50 steps (43 tests total passing) |
| 13 | feat(optim): Adam from scratch (bias-corrected) | [x] | `optim/adam.py`: single shared `t` for bias correction (not per-parameter) — open question to revisit at commit 17, since a revived element's reset m/v=0 gets bias-corrected against the *global* t rather than a fresh t=1, which under-corrects its first post-revival step; doesn't violate the mandatory commit-18 test (which only checks m/v are reset, not bias-correction step size) but worth flagging then. `tests/test_adam.py`: hand-computed t=1 check, multi-step vs reference loop, no-grad skip, zero_grad, real training loop reduces loss (48 tests total passing) |
| 14 | feat(train): mini-batch loop + spirals dataset | [x] | `train/dataset.py` (make_spirals), `train/loop.py` (dataset-agnostic mini-batch loop, asserts finite loss every step). Full-stack run on the real 2-128-128-3 He/ReLU/Adam architecture: no NaNs, loss drops, 99.7% train accuracy. `tests/test_train_loop.py` (50 tests total passing) |
| 15 | feat(train): learning-curve logging + Part-2 reproduce script | [x] | `train/loop.py` gained optional `on_epoch_end` callback (backward compatible). `train/run_part2.py`: full reproduce script, real run gives 99.7% accuracy, plot+CSV generated into `results/` but left untracked until commit 25 as planned. `tests/test_train_loop.py` (+callback test), `tests/test_run_part2.py` (redirects to tmp_path so tests never clobber the real results) — 52 tests total passing |
| 16 | feat(prune): boolean mask as graph node; w_eff = w * mask | [x] | `nn/linear.py`: mask baked permanently into Linear (`w_eff = weight*mask`, all-ones by default), reusing the existing `mul()` op's backward unchanged — zero new engine code needed for dL/dweight=mask*grad. `prune/mask.py` (`set_mask`). `tests/test_mask.py`: exact-zero masked grad, weight-change-doesn't-affect-output proof, finite-diff confirms masked entries independently, end-to-end (57 tests total passing) |
| 17 | feat(optim): mask-aware Adam — skip masked m/v/w, reset on revive | [x] | The critical-correctness commit. Extended existing `Adam` (not a separate class) with optional `masks` param (default None = old behavior, verified exact regression). `reset_state(param, indices)` for revival. `nn/linear.py`/`nn/sequential.py` gained `masked_parameters()`. Manually verified: masks=None reproduces plain Adam exactly, masked weight/m/v frozen across 20 steps despite constant nonzero grad, m/v don't decay (stay at frozen value, not drift to 0), revive+reset_state updates normally after. Formal pytest deferred to commit 18 as planned |
| 18 | test(prune): masked weight stays 0, moments don't drift, revive resets m/v | [x] | The single most important test in the repo. `tests/test_masked_adam.py`: exact-zero + arbitrary-nonzero frozen weight, m/v bitwise-unchanged (not decaying) while masked, revive+reset_state matches a hand-derived fresh-Adam-step formula exactly, a contrast test proving skipping reset_state actually changes the outcome (the bug, demonstrated not just guarded against), and a real Linear+backward() end-to-end run (63 tests total passing) |
| 19 | feat(prune): magnitude criterion (baseline) | [x] | `prune/criteria.py` (`magnitude_scores` = \|w\|), `prune/mask.py` gained `keep_mask_from_scores` (criterion-agnostic top-k selection, exact budget via argsort not threshold — reused by saliency in commit 20). `tests/test_magnitude_pruning.py`: exact sparsity count across fractions, keeps highest not lowest scores, tie handling, real Linear end-to-end with masked-grad check (68 tests total passing) |
| 20 | feat(prune): \|w·g\| saliency criterion with batch accumulation | [x] | `prune/criteria.py`: `saliency_scores` (\|w*grad\|) + `accumulate_gradients` (sweeps a dataset accumulating .grad via repeated backward(), no zero_grad/step — leaves .grad dirty on purpose, callers must clear it). `tests/test_saliency_pruning.py`: formula check, single-batch==plain-backward, multi-batch sums correctly, and the key test proving saliency and magnitude can rank connections oppositely (74 tests total passing) |
| 21 | feat(prune): cubic sparsity schedule + hard budget enforcement | [x] | `prune/schedule.py` (`cubic_sparsity`, Zhu & Gupta 2017 formula). `prune/mask.py` gained `prune_to_sparsity` (monotonic, exact-budget; reuses `keep_mask_from_scores` via forcing already-pruned entries to score -inf rather than duplicating top-k logic; asserts against decreasing target since there's no regrowth). `tests/test_schedule.py`: boundary conditions, formula match, prunes-faster-early-than-late property, never revives even when given fake high scores, rejects decreasing target, full multi-step simulated run (82 tests total passing) |
| 22 | feat(train): Part-3 self-pruning run to target sparsity + report | [x] | `train/loop.py` gained `on_step_end` hook (step-granularity, alongside `on_epoch_end`). `train/run_part3.py`: full self-pruning run wiring cubic schedule + saliency (default) + mask-aware Adam + `prune_to_sparsity` per layer. Real run: 90% sparsity, 99.78% accuracy. Found and fixed a real bug along the way — `prune_to_sparsity`'s old hard assert against decreasing target broke on rounding overshoot (continuous schedule vs discrete counts); now clamps to a no-op instead of erroring, with a regression test. `tests/test_run_part3.py` (85 tests total passing) |
| 23 | feat(eval): active-param/FLOP counter + honest dense×mask cost note | [x] | New `evaluation/` package (named to avoid shadowing the `eval` builtin, despite the `feat(eval)` commit scope). `evaluation/cost.py`: `weight_sparsity` (matches prune/'s own definition exactly), `active_param_count`/`total_param_count`, `model_flops` (dense vs theoretical-sparse), `cost_report`. The honest claim is *measured*, not just asserted: `test_dense_matmul_speed_unaffected_by_sparsity` times a 0%- vs 90%-sparse layer and finds no speedup (ratio ≈0.93). `train/run_part3.py` refactored to reuse `weight_sparsity` instead of its own local closure (91 tests total passing) |
| 24 | feat(eval): Pareto sweep + magnitude-vs-saliency, N seeds, mean±std | [x] | `train/run_pareto_sweep.py`: orchestrates many `run_part3` calls (2 criteria × 6 sparsities × 5 seeds) and aggregates mean±std accuracy. Fixed a real matplotlib figure leak found while testing (run_part2/run_part3 never closed figures — fine for one-off scripts, but the sweep calls them dozens of times) by adding `plt.close(fig)`. Real sweep result: both criteria near-flat through 90% sparsity; at 95%, magnitude degrades and becomes unstable across seeds (std≈0.030, worst seed dropped to 92.1%) while saliency stays flat (std≈0.0008) — a genuine, falsifiable finding for commit 25. `tests/test_pareto_sweep.py` with a tiny fast config (95 tests total passing) |
| 25 | docs: commit plots + raw CSVs; falsifiable claim | [x] | Committed all `results/` artifacts (Part-2 + Part-3 learning curves, Pareto raw + summary CSVs, Pareto plot) that were deliberately left untracked since commit 15. `results/CLAIM.md`: falsifiable claim about saliency's stability vs magnitude's instability at 95% sparsity, with exact numbers, an explicit falsification procedure, and a stated scope |
| 26 | docs: DESIGN.md | [x] | Four sections per spec scope: criterion derivation (first-order Taylor, |w·g|), masked-grad/mask-aware-Adam design (mul()'s existing backward gives masked-grad-zero for free; zero-grad ≠ frozen Adam state; the global-t revival trade-off named explicitly), the bottleneck (dense×mask measured no-speedup, what a real speedup needs), and serving (memory win is real today, compute win needs structured sparsity). Cross-checked every cited test name and number against the actual test files before writing them down |
| 27 | docs: README run commands, uv setup | [x] | Filled in all run commands (gradcheck/Part2/Part3/Pareto), added `evaluation/` to the layout table, plain-venv fallback for uv, links to DESIGN.md/CLAIM.md. Verified every command actually runs as documented; re-running Part2/Part3 reproduced identical numbers (0.9967/0.9978/0.90 sparsity) confirming the reproducibility claim, not just asserting it. All 27 commits done |

Commits 28-39 close two gaps found in a later re-check against the original spec: a real (not just disclosed) cost measurement via structured/neuron-level pruning + compressed inference, and full dynamic sparse training (regrowth). See `using-this-prompt-concurrent-manatee.md` plan for the full design rationale.

| 28 | feat(prune): structured (neuron-level) magnitude/saliency criteria | [x] | `prune/criteria.py` gained `neuron_magnitude_scores`/`neuron_saliency_scores` (axis=0 sum over each output column, since `weight.shape == (in_features, out_features)`). `tests/test_structured_criteria.py`: shape check on a deliberately non-square layer (catches axis mixups loudly), formula match, gradient-required assertion, and a neuron-level rank-disagreement test mirroring commit 20's key per-weight test (99 tests total passing) |
| 29 | feat(prune): structured masking — prune_neurons_to_count | [x] | `prune/mask.py` gained `keep_neurons_from_scores`/`prune_neurons_to_count` (column-wise `-inf` trick, mirrors `prune_to_sparsity` but as a parallel function -- that function and its adversarial never-revives test are untouched). `tests/test_structured_masking.py`: exact neuron-count budget, never-revives adversarial test, increasing-target no-op, the explicitly-flagged mixed unstructured-then-structured scenario (half-dead column still counts active until fully zeroed), and a forward/backward sanity check confirming the existing per-entry masked-grad machinery needs no changes (105 tests total passing) |
| 30 | feat(prune): compress_model — cross-layer dense compression | [x] | New `prune/compress.py`: `CompressedLinear`/`CompressedReLU`/`CompressedSequential` (plain ndarrays, inference-only) + `compress_model`, which threads each Linear's alive-output-index set into the next Linear's input-row slicing (ReLU(0)==0 passes deadness through unchanged), never slices the first layer's input or the last layer's output. `tests/test_compress.py`: numerically exact vs. masked dense forward (`atol=1e-12`) across 0%, partial, and high structured sparsity incl. a middle layer down to 1 neuron; explicit shrunk-shape check; last-layer-output-never-sliced even when adversarially masked (bias, never pruned, still must match exactly); no-aliasing in both directions (mutate compressed, mutate original, neither leaks); unsupported layer type raises (112 tests total passing) |
| 31 | feat(eval): wall-clock benchmark proving structured compression is actually faster | [x] | `evaluation/cost.py` gained `time_dense_vs_compressed_forward`. New test in `tests/test_cost.py` prunes both 128-wide hidden layers of the spirals MLP to 32/128 active neurons (75% structured sparsity) and times dense-masked vs. compressed forward: measured ratio ≈0.245 (a real ~4x speedup), stable across 5 repeated runs -- directly contrasted against the existing unstructured test's ratio ≈0.93 (no speedup). This is the actual Part-4 "real cost measurement" evidence the project was missing (113 tests total passing) |
| 32 | feat(train): Part-4 structured self-pruning run + structured-vs-unstructured comparison | [x] | New `train/run_part4_structured.py` (single structured run, excludes the final layer's output, in-script compress_model sanity check) and `train/run_structured_vs_unstructured.py` (orchestrates run_part3 + run_part4_structured across sparsities/seeds, mirrors run_pareto_sweep.py). **Found and fixed a real bug running the actual script** (caught by the in-script `np.allclose` sanity check failing, not a unit test -- the earlier unit tests in commit 30 happened to use freshly-constructed layers whose bias starts at exactly 0, which accidentally hid this): bias has no mask of its own, so a structurally-pruned neuron's bias kept drifting via stale Adam momentum after its weight column froze, leaving `ReLU(0+bias)` nonzero for a "dead" neuron -- exactly the zero-gradient-isn't-frozen-state lesson mask-aware Adam already encodes for weight, rediscovered for bias. Fixed via a new `bias_mask` on `Linear` (synced by `prune.mask.set_mask`, wired into `masked_parameters()`), plus an explicit zero in `prune_neurons_to_count` at the moment of death; regression tests added to `tests/test_mask.py` and `tests/test_masked_adam.py`. Real runs: single structured run reaches 68.7% sparsity at 99.78% accuracy (`results/part4_structured_self_pruning.{csv,png}`); the comparison sweep (3 sparsities × 3 seeds × 2 methods) found structured and unstructured saliency pruning statistically indistinguishable on this task (~99.7% both, std ≈0.001) up to 85% -- the expected "structured costs more accuracy" penalty didn't show up, consistent with this task's known overparameterization (`results/structured_vs_unstructured.csv`) (122 tests total passing) |

Commits 33-38 build dynamic sparse training (regrowth), the second gap from the same re-check: a `revive_to_count` primitive existed only conceptually until now -- these commits make it real and wire it into an actual training run.

| 33 | feat(nn): store w_eff on Linear for external gradient inspection | [x] | One-line change in `nn/linear.py`: `w_eff` (the `weight*mask` graph node) is now `self.w_eff`, not a throwaway local -- its `.grad` after `backward()` is the dense/unmasked gradient signal (mask only gets applied one step later, inside `mul()`'s backward), needed by regrowth scoring. `tests/test_mask.py` gained a test proving `w_eff.grad` is nonzero at a masked entry where `weight.grad` is exactly 0, confirming the design insight in practice, not just by code-tracing (123 tests total passing) |
| 34 | feat(prune): accumulate_dense_gradients — multi-batch w_eff.grad accumulation | [x] | `prune/criteria.py` gained `accumulate_dense_gradients`, a parallel function to `accumulate_gradients` (not a modification -- different return type, different callers). Unlike `weight` (the same persistent Tensor every forward call, so `weight.grad` naturally sums via `+=`), `w_eff` is rebuilt fresh every `__call__`, so its `.grad` doesn't accumulate on its own; this explicitly sums `layer.w_eff.grad` right after each batch's `backward()`, before the next forward call's fresh `w_eff` overwrites it. `tests/test_accumulate_dense_gradients.py`: single-batch matches plain backward, multi-batch sum matches a hand-summed reference (mirrors commit 20's equivalent test), returns every prunable layer, and the key proof that the accumulated total is nonzero at a masked entry where `weight.grad` is exactly 0 (128 tests total passing) |
| 35 | feat(prune): revive_to_count — mask regrowth primitive | [x] | `prune/mask.py` gained `revive_to_count`: mirrors `prune_to_sparsity`'s `-inf` trick inverted (already-active entries forced to `-inf` so top-k can only select among masked-off ones), returns the revived boolean index array for the caller to pass to `optimizer.reset_state` and to exclude from the same DST cycle's drop step. Doesn't touch the optimizer or `weight.data` itself -- mask mechanics only, same division of responsibility `prune_to_sparsity` already has. `tests/test_revive.py`: adversarial never-selects-active test, exact budget, clamping when too few masked entries exist, zero-revive edge case, keeps-highest-scoring proof, weight value untouched (mask gates, never destroys), and an explicit regression check that `prune_to_sparsity`'s never-revives contract still holds untouched (135 tests total passing) |
| 36 | feat(prune): run_exchange_cycle — grow+drop, net-zero active count | [x] | New `prune/dst.py`. **Caught and fixed a real bug during development** (before committing, via manual verification, not after a failing test): a first draft used a single "excluded -> +inf" bucket lumping revived and still-inactive entries together for the drop step's top-k selection -- this silently breaks whenever there are more excluded entries than the keep budget, since top-k can't tell "force this in" apart from "force this out" if both share the same score. Correct version needs three-way scoring: revived entries forced to `+inf` (must survive this cycle), still-inactive entries forced to `-inf` (never reactivated by the drop step), only the untouched originally-active entries get real `drop_scores`. `tests/test_dst.py`: net-zero active count (incl. clamped revive), the exclusion landmine proven directly (a freshly-revived entry survives despite the worst possible drop score), integrated Adam moment-reset through the real `run_exchange_cycle` call path (not just `reset_state` in isolation), and the **repeated prune→revive→prune→revive stability test** (5 cycles on one connection: weight value frozen exactly while masked and unchanged by revival itself, moments reset to exactly 0 on every single revival not just the first, nothing ever non-finite, and the final post-revival update matches a hand-derived fresh-Adam-step formula exactly -- no path-dependent residue survives the thrashing) -- this is the concrete, measured answer to "reason about the stability implications of regrowth" (139 tests total passing) |
| 37 | feat(prune): dst_step — ramp-then-maintain scheduling | [x] | `prune/dst.py` gained `dst_step`: before `prune_end_step`, behaves identically to `run_part3`'s existing non-regrowth `on_step_end` (accumulate_gradients + prune_to_sparsity toward the cubic target); at/after `prune_end_step`, switches to periodic `run_exchange_cycle` calls sized as `exchange_fraction * n_active` per layer, using `accumulate_dense_gradients`'s signal to grow and the existing criterion to drop. Maintenance phase runs two full dataset sweeps per cycle -- noted explicitly as a known, accepted v1 inefficiency, not silently absorbed. `tests/test_dst.py` gained: a byte-exact cross-check between `dst_step`'s ramp branch and the manual `accumulate_gradients`+`prune_to_sparsity` equivalent on an identically-weighted second model (regression-style, not just "looks similar"), and a maintenance-phase test holding each layer's active count exactly constant across 10 consecutive cycles, not just checking the endpoint (141 tests total passing; one pre-existing wall-clock microbenchmark flaked once under load, confirmed stable on rerun in isolation) |
| 38 | feat(train): enable_regrowth opt-in + DST comparison run | [x] | `train/run_part3.py::main` gained `enable_regrowth`/`exchange_fraction` (default off; `on_step_end` branches to `dst_step` when enabled, falls through to the original code unchanged otherwise). Regression test confirms explicit `enable_regrowth=False` is byte-for-byte identical to omitting it. New `train/run_dst_comparison.py` (mirrors `run_pareto_sweep.py`'s orchestration pattern): runs `run_part3` with/without regrowth across 5 seeds at 90% and 95% sparsity (the regime DST is supposed to help most, per the literature). **Real, measured result -- not what was expected, reported honestly anyway**: regrowth made accuracy substantially *worse* and far less stable on this task: 90% sparsity 99.67%±0.0007 (no regrowth) vs 94.89%±0.0468 (with regrowth); 95% sparsity 99.53%±0.0019 vs 91.02%±0.1159, worst seed dropping to 68.3% (`results/dst_comparison.csv`). Plausible explanation, not yet independently verified: plain saliency pruning already converges to a near-optimal fixed mask quickly on this small, heavily-overparameterized task (per `results/CLAIM.md`), so continuously disturbing that mask via exchange cycles adds churn the small amount of training between cycles can't recover from, rather than the literature's usual regime (a large/hard task where the early fixed mask itself is suboptimal and benefits from reallocation). Caught one test bug along the way verifying this (not a production bug): comparing stale post-training `.grad` against a mask that the *final* exchange cycle had just changed, instead of a fresh forward/backward -- same pattern as `test_run_part3_pruned_model_respects_masked_grad` already uses correctly (146 tests total passing) |

Commits 39+ fix concrete, executed-and-verified issues found in a hostile "Staff AI Platform Review" pass over the whole repo (re-derived every op's math by hand, ran adversarial repros against the actual code rather than trusting comments/tests/docs). Each row cites the exact repro that proved the issue real before it was fixed.

| 39 | fix(prune): remove redundant gradient sweep in dst_step maintenance phase | [x] | The review traced `dst_step`'s maintenance branch calling both `accumulate_dense_gradients` *and* `accumulate_gradients` on the same data, with the docstring excusing it as "could be fused later." Verified by direct execution that `accumulate_dense_gradients`'s backward() pass already leaves `weight.grad` bit-for-bit identical to what a standalone `accumulate_gradients` call would produce (same backward computation — `mul()` writes both `weight.grad` and the source of `w_eff.grad` from one pass) — the second call was pure waste, not "fusable future work." Removed it; added `test_dst_step_maintenance_phase_does_one_sweep_not_two` (monkeypatches `accumulate_gradients` to raise if dst_step ever calls it again during maintenance) so this can't silently regress (147 tests total passing) |
| 40 | fix(prune): guard run_exchange_cycle no-op when a layer is fully dead | [x] | The review constructed an adversarial repro: at `n_active_before == 0`, the drop step's keep-target is also 0, so `_top_k_keep_mask(scores, n_keep=0)` returns all-False *regardless of score* — even a `+inf`-forced "must survive" entry gets dropped, silently violating `run_exchange_cycle`'s own documented "revived entries survive their first cycle" guarantee (confirmed by direct execution: the function revived an entry then immediately dropped it again in the same call). Not reachable in any of this project's actual configured runs (final_sparsity never hits 1.0), but a real, demonstrable gap. Fixed with an explicit `if n_active_before == 0: return` guard — there's nothing to exchange when nothing is active, so skipping is the correct semantics, not just crash-avoidance. `test_run_exchange_cycle_is_a_no_op_on_a_fully_dead_layer` pins the exact repro (148 tests total passing) |

## Workflow per commit

1. Check [Common_pittfall.md](Common_pittfall.md) for the section(s)
   relevant to the feature about to be built.
2. Implement following [CODING_GUIDELINES.md](CODING_GUIDELINES.md)
   (forward + backward + shape reasoning + numerical-stability notes for
   every op; explicit unbroadcasting; `+=` accumulation never overwrite;
   no destructive in-place mutation of parameters unless asked).
3. Write/extend tests for the feature (gradcheck where applicable) and run
   them — don't mark done on faith.
4. Commit, then update this file's status to `[x]` with the commit hash.
